% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_design.R
\name{build_delta_channel}
\alias{build_delta_channel}
\title{Build the DELTA association channel's fixed-effects design:
\code{delta(t) = X(t) - X(0)} evaluated at the survival time and at each
quadrature node. For a pure random INTERCEPT (\code{random_effects =
"intercept"}), the random-effect contribution to delta is always exactly
zero (the intercept doesn't depend on time, so it cancels in the
subtraction) - this function therefore only handles the fixed-effects
part; the caller should not add a random-effect term for this channel
under that scope.}
\usage{
build_delta_channel(long_formula, time_var, X_time_surv, X_time_quad)
}
\arguments{
\item{long_formula}{The longitudinal fixed-effects formula.}

\item{time_var}{Name of the time variable in long_formula.}

\item{X_time_surv}{Already-built value-channel design at the survival
times (\code{[N_sub, p]}), from \code{build_time_design()}.}

\item{X_time_quad}{Already-built value-channel design at the quadrature
nodes (\code{[N_sub, n_quad, p]}), from \code{build_time_design()}.}
}
\value{
A list with \code{X_delta_surv} (\code{[N_sub, p]}) and
  \code{X_delta_quad} (\code{[N_sub, n_quad, p]}).
}
\description{
Build the DELTA association channel's fixed-effects design:
\code{delta(t) = X(t) - X(0)} evaluated at the survival time and at each
quadrature node. For a pure random INTERCEPT (\code{random_effects =
"intercept"}), the random-effect contribution to delta is always exactly
zero (the intercept doesn't depend on time, so it cancels in the
subtraction) - this function therefore only handles the fixed-effects
part; the caller should not add a random-effect term for this channel
under that scope.
}
\keyword{internal}
